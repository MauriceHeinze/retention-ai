{
  "name": "retention-ai",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @retention-ai/frontend dev",
    "build": "pnpm -r build",
    "lint": "pnpm --filter @retention-ai/frontend lint"
  }
}
