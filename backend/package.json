{
  "name": "retention-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts",
    "agent:smoke": "node --env-file=.env.local --import tsx src/smoke.ts",
    "stripe:seed": "node --env-file=.env.local --import tsx src/seed-stripe.ts",
    "stripe:smoke": "node --env-file=.env.local --import tsx src/smoke.ts --stripe"
  },
  "dependencies": {
    "@openrouter/ai-sdk-provider": "^3.0.0",
    "ai": "^7.0.99",
    "stripe": "^22.6.2",
    "zod": "^4.6.4"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
